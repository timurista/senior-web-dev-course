/**
 * @param {string} s
 * @param {number} k
 * @return {number}
 */
var lengthOfLongestSubstringKDistinct = function(s, k) {
  // start middle of string
  // wiggle left
  // wiggle right
  // if length
  let map = {};
  // for (let i = s) {

  // }
};
